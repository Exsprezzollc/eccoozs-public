ECCOOZS Vocabulary Adventure — Static Site 2.16.0

This directory contains the already-built public site.
Serve its contents from the ROOT of a stable HTTPS origin.

Recommended use:
- For Vercel, use the FULL WEB RELEASE source package instead; it includes vercel.json and performs npm run build automatically.
- For a generic static host, publish all files in this folder at the origin root.

Important: learner progress is stored in browser localStorage for the exact origin. Keep the final public URL stable.
