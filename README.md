# ECCOOZS V6 static baseline

This branch intentionally contains only the uploaded V6 public-site payload, its static assets, and the build script required to serve it. The previous React/Next welcome implementation is not part of this tree.
